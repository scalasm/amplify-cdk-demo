version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd amplify-demo
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: "amplify-demo/build"
    files:
      - "**/*"
  cache:
    paths:
      - "amplify-demo/node_modules/**/*"
